[defaults]
# Use local inventory file
inventory = inventory

# Optional: Other useful defaults
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
# Always ask for sudo password when become is used
become_ask_pass = True